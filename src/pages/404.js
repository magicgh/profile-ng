import React from 'react';
import * as sty from '../styles/global';

export default function NotFound() {
    return (
        <sty.section>
            <sty.section.heading>Oops, we couldn't find this page!</sty.section.heading>
        </sty.section>
    );
}